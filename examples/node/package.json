{
  "name": "@collect/test",
  "private": "true",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist/**/*.{js,d.ts,mjs}"
  ],
  "scripts": {
    "dev": "tsup --watch --onSuccess 'node dist/index.js'",
    "build": "tsup",
    "types:check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@collect/sdk": "workspace:*"
  },
  "version": "0.0.0",
  "publishConfig": {
    "access": "restricted"
  }
}
