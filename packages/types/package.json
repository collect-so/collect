{
  "name": "@collect.so/types",
  "description": "Collect TypeScript types",
  "license": "MIT",
  "version": "0.14.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist/**/*.{js,d.ts,mjs}",
    "./package.json"
  ],
  "scripts": {
    "build": "pnpm clean && tsc -d --declarationMap",
    "clean": "rm -rf ./dist ./node_modules/.cache",
    "test": "echo \"Error: no test specified\" && exit 0",
    "flush": "rm -rf node_modules"
  },
  "dependencies": {},
  "peerDependencies": {},
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "https",
    "url": "https://github.com/collect-so/collect-toolkit"
  }
}
